// 预留AI接口，可用于生成剧情、结局等
export async function fetchAIResult(prompt) {
  // 示例：调用AI API
  // const response = await fetch('https://api.openai.com/v1/chat/completions', { ... });
  // return await response.json();
  return { result: 'AI功能待接入' };
}
